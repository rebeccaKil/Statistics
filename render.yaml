services:
  - type: web
    name: ai-excel-analyzer-backend
    runtime: docker
    repo: https://github.com/rebeccaKil/Statistics
    branch: main
    dockerfilePath: backend.Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8080
    healthCheckPath: /health

